<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="be.heh.std">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GlobaleVariable"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProcessimProject"
        tools:targetApi="31">
        <activity
            android:name=".app.LiquidActivity"
            android:exported="false" />
        <activity
            android:name=".app.PillsActivity"
            android:exported="false" />
        <activity
            android:name=".app.NewPlcActivity"
            android:exported="true" />
        <activity
            android:name=".app.ListPlcActivity"
            android:exported="true" />
        <activity
            android:name=".app.ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".app.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".app.LoginActivity"
            android:exported="true" />
    </application>

</manifest>